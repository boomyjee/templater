@ global.header = type_mixin(".header");

.header.default(data) {
    @ data = extend({padding:"20px",background:{color:"#eee"}},data);
    .background(data.background);
    .container {
        padding: @data.padding;
        > h2 {
            margin: 0;
        }
    }
}